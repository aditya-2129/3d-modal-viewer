version: "3.9"

services:
  cad-redis:
    image: redis:7-alpine
    command: redis-server --requirepass securepassword123
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  web:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      - cad-redis
    volumes:
      - uploads:/uploads
    env_file: .env
    environment:
      REDIS_URL: ${REDIS_URL:-redis://:securepassword123@127.0.0.1:6379}
      UPLOAD_DIR: ${UPLOAD_DIR:-/uploads}

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    restart: unless-stopped
    command: npx tsx workers/freecad.worker.ts
    depends_on:
      - cad-redis
    volumes:
      - uploads:/uploads
    env_file: .env
    environment:
      REDIS_URL: ${REDIS_URL:-redis://:securepassword123@127.0.0.1:6379}
      UPLOAD_DIR: ${UPLOAD_DIR:-/uploads}

volumes:
  redis_data:
  uploads:

networks:
  default:
    name: cad-app-network
